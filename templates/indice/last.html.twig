{% extends '@AcMarcheIssep/layout.html.twig' %}
{% block body %}

    <div class="card">
        <h3 class="card-header">{{ indices|length }} indices sur les 6 derniers mois</h3>
        <div class="card-body">
            {% include '@AcMarcheIssep/_urlexecuted.html.twig' %}
            <table class="table table-bordered">
                <thead>
                <tr>
                    <th>Config ID</th>
                    <th>Nom</th>
                    <th>Date</th>
                    <th>AqiValue</th>
                </tr>
                </thead>
                {% for indice in indices %}
                    <tr>
                        <td>
                            {{ indice.configId }}
                        </td>
                        <td>
                            {{ indice.pointName }}
                        </td>
                        <td>
                            {{ indice.ts|format_datetime('full','medium') }}
                        </td>
                        <td>
                            {{ indice.aqiValue }}
                        </td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </div>
{% endblock %}